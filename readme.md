# Installation

From your `$HOME` directory:

```BASH
git clone https://github.com/bdelespierre/dotfiles.git .dotfiles
.dotfiles/install.sh
```

Then you may stow:

```BASH
cd .dotfiles
stow bash
stow git
stow vim
```

Enjoy!
